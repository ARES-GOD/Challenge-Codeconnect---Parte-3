import React, {type FC } from "react";

interface IProps {};

const Home:FC<IProps> = (props) => {
    return <>
        <div>PRUEBA HOME</div>
        <button>Home</button>
    </>
};

export default Home;